const AutoStart = Buffer.from([0xaa, 0x10, 0x01, 0x9F]);

const StepStart = Buffer.from([0xaa, 0x10, 0x02, 0x96]);

const Stop = Buffer.from([0xaa, 0x10, 0x03, 0x91]);

const EStop = Buffer.from([0xaa, 0x10, 0x04, 0x84]);

export { AutoStart, StepStart, Stop, EStop }
